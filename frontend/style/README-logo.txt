Place the Peony logo image you provided at:

frontend/style/peony-logo.png

Recommended size: width 140–300px (the CSS uses 140px in the sidebar).
If you prefer SVG, name it `peony-logo.svg` and update the `src` in `frontend/dashboard.html` accordingly.
